# CS2 Clutch

Redesign of a live CS2 skin platform, built as a twelve stage product design pipeline. Working placeholder name. Baseline: daddyskins.com, the founder's own product, six years live.

**Live: [project roadmap](https://sergiodesign4u-dot.github.io/cs2-clutch/)**

Every stage leaves two outputs: a markdown source of truth in `docs/` for the model, and an html page in the browser for the person who decides. Structure and information architecture are inherited from the baseline, and so is the colour register since 11 August 2026. Typography, shape language, motion and copy are derived at stage 06.

## Status

This table and `_nav.js` are the only two places where status lives. Nothing else records it.

| # | Stage | Pages | Status |
|---|-------|-------|--------|
| 01 | Foundation Research | [Research](https://sergiodesign4u-dot.github.io/cs2-clutch/research/research.html), [Baseline walk](https://sergiodesign4u-dot.github.io/cs2-clutch/research/baseline.html), [Baseline behind the account](https://sergiodesign4u-dot.github.io/cs2-clutch/research/baseline-account.html) | Done. **Baseline re-walked 18 Aug 2026 and the signed-in half captured** |
| 02 | User Research (Personas, JTBD) | [Personas](https://sergiodesign4u-dot.github.io/cs2-clutch/research/personas.html), [JTBD](https://sergiodesign4u-dot.github.io/cs2-clutch/research/jtbd.html) | Done |
| 02+ | CJM (As-Is, To-Be) | [CJM As-Is](https://sergiodesign4u-dot.github.io/cs2-clutch/research/cjm-as-is.html), [CJM To-Be](https://sergiodesign4u-dot.github.io/cs2-clutch/research/cjm-to-be.html) | Done |
| 03 | Information Architecture (Base, Detail) | [Flows](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/flows.html), [Concept map](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/concept-map.html), [Sitemap](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/sitemap.html), [Structure](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/structure.html), [0.1 Navigation](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/navigation.html), [0.2 Footer](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/footer.html), [0.11 Published numbers](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/numbers.html), [0.12 Markets](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/markets.html), [0.13 SEO](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/seo.html), [0.14 Round proof](https://sergiodesign4u-dot.github.io/cs2-clutch/ia/round-proof.html) | In progress. Base layer done, Detail layer complete, **26 of 26 node pages, 68 map nodes of which 52 are MVP**, the count read from `ia/docs/sitemap.md`, which owns it, and corrected here on 23 Aug 2026 after `D-90` added `7.3` and this table went on saying 67 and 51, `D-60` of 21 Aug 2026 having dissolved `3.4` when stock left the product and `D-64` of the same day having carried that withdrawal through the six live files it had not reached, `numbers.md` above all, where the register's own **withdrawn** state was used for the first time, `D-36` of 20 Aug 2026 having added `5.9` Roll history, `5.10` Profile and `5.11` Settings so the account menu's rows have destinations, block bank built, two-instrument audit run. **Amended 18 Aug 2026 by D-24 to D-26:** the case tile rebuilt, Home took the baseline's shape, the sign-in consent gate added, identity verification parked as LATER. **Amended 23 Aug 2026 by `D-91` and `D-92`:** withdrawal is a purchase and the settlement lands on `5.1`, `5.3`, `3.3` and `3.6`, then the market it is struck against is named and the answer is us. **Five node docs carry it**, `0.11` above all, where **section 2c restates row `A1` for this product and returns partly satisfied rather than closing it**, the venue rows on `0.11`, `5.1`, `5.3` and the map close with their arguments kept, **the per-case gap stays `[?]` with a decision behind it rather than a dependency**, and three narrower holes replace the one that closed: how far under Steam we set a copy, what the outbound link says it is for, and whether the copies are in fact ours at withdrawal time |
| 04 | Wireframes | [Overview](https://sergiodesign4u-dot.github.io/cs2-clutch/wireframes/overview.html), [1.0 Home](https://sergiodesign4u-dot.github.io/cs2-clutch/wireframes/index.html), [1.1 Home, account exists](https://sergiodesign4u-dot.github.io/cs2-clutch/wireframes/index-account.html), [3.3 Case screen](https://sergiodesign4u-dot.github.io/cs2-clutch/wireframes/case.html), [3.5 The open](https://sergiodesign4u-dot.github.io/cs2-clutch/wireframes/case-open.html), [3.6 The outcome](https://sergiodesign4u-dot.github.io/cs2-clutch/wireframes/case-outcome.html), [2.4 Sign in](https://sergiodesign4u-dot.github.io/cs2-clutch/wireframes/signin.html) | In progress. ****122 of 122 pages built, and stage 04 has no unbuilt page left****, node `3.1` having been redrawn the same day by `D-65` after the founder pointed at the live catalogue: **the shelf is a run of named category sections under a sticky jump bar, with the search and the filter inside the bar and a filter drawer holding five controls and a sort.** **Three of that node's rows were wrong about our own product** and two of them reverse refusals it had made without opening the page, the risk-band filter and the sort. The category list was never `[?]`: `baseline.md` has held all thirteen names since 11 Aug 2026 and the node said `[?]` beside them, which is how an empty category bar shipped. **Then `D-66` the same day:** the filter control now opens the drawer rather than being a picture of one, **the process prose came off the wires and stayed in the IA** under a rule that binds every screen from here, a wireframe states a state and never cites a decision record, and the founder's signed-in capture showed an **Additional** group the pre-login walk could not reach, **which cut against a refusal written three hours earlier**. **Then `D-67`:** the daily tier ladder came to the shelf as the baseline runs it, and with a second consumer it became component `0.15`. **`home.md` had refused a component in as many words**, on the ground that a ladder that becomes a component is a ladder that spreads, **and that sentence is the reason it had to become one**: boundaries attached to a node stay behind when the markup travels. The same check found a factual error inside `D-25`, printed rather than quietly corrected: it shipped the ladder to Home **as the baseline does it**, and the baseline's Home has no ladder. **And the descriptions came off every wire, not just the catalogue:** 31 decision citations across 17 of 29 files, all removed from rendered text and none from the comments. **Then `D-68`:** the daily case lost its tile, so the Daily section is the ladder and nothing else. **That reverses section 2 of the node and the section is kept whole, because its argument is the cost:** a banner survives every filter because it is not part of the list, and a case does not, so the Daily section renders in every state including `3.2` when nothing matches at all. **It closes a debt rather than opening one:** where the once-a-day limit renders is answered, no field does, and `0.7`'s two daily states are void. **Node `7.1` the public result drawn on 21 Aug 2026 in six pages, `D-69`**, on the same rule that moved the catalogue: `result.html` was referenced from sixteen files and from `_nav.js` and returned a 404 on all thirty pages, because the live feed lands every one of its twenty four tiles there. **The recomputed-and-mismatched state is drawn on purpose and early**, since it is the one where building late costs most: our own proof failing in a stranger's browser on the surface that travels furthest. `0.14` variant V3 is a built component now. **The founder answered how an account appears in public** and the answer put two of our own decisions against each other: `0.8` gives the feed's avatar a profile and `7.1` refuses one, and no node exists for it either way. **Node `2.1` the geo gate drawn on 21 Aug 2026 in five pages, `D-70`, and the main flow is now drawn end to end.** **`D-26` had reached that node's header and not its body:** it went on specifying the 18+ declaration as blocks 2 to 4 for three days after the header said it had moved to `2.4`. **What is left is smaller than a dialog:** on an open market the node renders nothing, and the interrupt exists only when the answer is no or carries a limit. The gate is built once in `_nav.js` and **fires from the case screen's own trigger, over the case screen**, because a gate that does not fire is a picture of a gate. **And the live feed runs the other way** by founder decision, reversed on its own rule rather than by flipping a keyframe two components share. **Node `5.1` account and inventory drawn on 21 Aug 2026 in three pages, `D-71`**, the front door of the cluster at the floor of the As-Is emotional map. **`D-40` had reached the map and not that node**, which said the rail carries four destinations and the bar rises to four: the third decision in two days found applied to a header and not a body. **Every item carries two dated figures and no third number made out of them**, and they never share a line at 360, because collapsing them is how a delta gets invented by a layout. **And one number was rendering two values on adjacent surfaces:** the header read 18.60 for the value of items held while the page rendered 130.60. Fixed by making the shell read the page, and the sweep now fails a page whose two renderings disagree. **Node `1.2` provably fair drawn on 21 Aug 2026 in six pages, `D-72`**, the largest single piece of product a person can read without an account. **The page with the form is the one page that never prints a verdict:** `0.14` forbids a badge in every variant and this is where the temptation is largest, so the sweep fails any of the six whose text contains verified, guarantee or certified. **Three failures, three states, and conflating any two is the defect:** malformed is a round we could not read, unavailable is one we can read and cannot prove yet, mismatched is one we can read, can prove, and our proof is wrong. **Nothing on the page is drafted with a plausible value:** the algorithm block renders three headings and three absences, because a published algorithm with an invented function in it is the one artefact a reader would compute against. **Node `5.3` withdrawal drawn on 21 Aug 2026 in seven pages, `D-73`**, the end of flow 3 and the page where the injury is not the duration but the unattributed silence. **The clock shows three real figures and never a fourth invented one:** elapsed, published ceiling, and who is being waited on. Never an ETA, never a bar, never a spinner. **And it is drawn against a mechanic the whole map does not model:** on the live product withdrawal is a purchase rather than a delivery, and in the observed row a real copy cost **six times** what the site said the win was worth, 27.52 against 4.48, **so the difference itself was 23.04, which is a little over five times the win rather than six.** Both readings of that sentence are in `research/docs/baseline-account.md` section 5 and the wireframes inherited the shorter one; **the ratio that is six is the market price, not the gap.** Drawn against the other reading, with the choice in front of the founder. **Node `4.1` deposit drawn on 22 Aug 2026 in five pages, `D-74`, and cluster 4 is complete.** **`D-26` had reached three files out of ten:** it took identity verification out of round 1 on 18 August and moved the surviving layer from before funding to before withdrawal, and node `4.1` still had a section titled "Reached through `2.7`", flow 2 still drew eight nodes of identity branch, and the sitemap still explained the free-entry hole with the old sequence in four places. **The geo gate is the sharpest instance: its header records the decision and its section 0, six lines below, contradicts the header.** **The promise inverts rather than disappearing:** block 1 could say the exit carries no check because one row put the check before funding and another forbade a branch on the exit, and with the surviving layer at the exit that sentence would be true today and false the day it lands, which is the rising-threshold barrier applied to a condition instead of to a number. **And this is the one screen an unpublished peg empties rather than degrades**, because the conversion is the block: a second gap fell out of it that no file had named, which real currency a deposit is taken in. **The ceiling refuses rather than dims**, extending `D-58` off the consent gate, and typing a different figure counts as the same consent. **The live strip comes off `4.2`**, whose own forbidden list already says no offer of any kind. **Node `6.1` responsible play drawn on 22 Aug 2026 in five pages, `D-75`, and cluster 6 is complete.** **The count was right and every file naming the members was wrong:** responsible play has had three entries since the base layer, and `responsible.md`, `footer.md` and `sitemap.md` all listed the rail, which `D-29` took it off on 19 August and `D-40` replaced with the account menu on 20 August. **The footer node says "three entries: the rail, the money control, and this band" six lines below its own sentence that responsible play has had no other carrier since `D-40` took it off the rail.** **A refusal survived its own citation:** block 2 refused the live ticker on the ground that it renders on `1.0` and `3.3` only, `D-59` deleted that ground, and the refusal now rests on the register's identical rule for the daily ladder, "`6.1` may never render it". **Guest and self-excluded controls are live and they answer**, the third application of `D-58` off its own surface and the sharpest, since a person inside a self exclusion who presses a control is owed the reason in words. **Three surfaces is a rule rather than three readings and it is carried as a candidate for `CLAUDE.md` at the stage close.** **And one shared rule was breaking any page with a long heading:** the section head gave its title `flex: 0 0 auto`, so a long H2 could neither shrink nor wrap and pushed the 96px rule past the edge, scrolling every one of the five sideways by 3px at 900 and latent everywhere else. **Node `0.10` support and appeals drawn on 22 Aug 2026 in nine pages, `D-76`.** **It is not a help centre with a form at the bottom, and the evidence says why:** the barrier under it is three reviews and not one of them is about slow support, they are about a decision with no ground and no route. So four things ship, and **the fourth is what earns the node: a state for our own published deadline being missed**, because a success signal with no state for its own failure is a number nobody has to meet. **`D-26` reached a fourth file and here it deleted a route rather than a rule:** `2.9` was one of five routes in, and what it takes with it is the only route that would have carried an identity ground into the appeal form, so the refusal of identity documents is now a rule with no live case and it stays written and asserted. **And two nodes answer one pattern differently:** `0.13`'s title pattern bakes in `CS2 Clutch`, `0.10` follows it and `6.1` refuses it, both are indexed, and neither is wrong on its own reasoning. **The page invents nothing it does not hold:** no deadline figure, no support address, no escalation authority, all three rendered as unset on all nine pages. **Node `0.9` legal and policy drawn on 22 Aug 2026 in five pages, `D-77`, and it found two defects in what had shipped hours earlier.** **The live strip had come off `0.10`'s nine pages with no parent:** `4.2` refuses it by its own forbidden list and `6.1` by the register's own rule, `0.10` refuses nothing, and **a wireframe cannot borrow another node's parent**. Reverted, with the argument for taking it off sent to the founder. **And `.wf-btn--small` is 32px while three nodes claim AAA at 44**, on sixteen pages; fixed as a hit area rather than as a box so no layout moves, **and the sweep had been measuring the element box**, which is why it survived four nodes that all state the rule. **The node is one template and four documents with only the terms drawn**, and its version history is the thing no competitor in this category has: a policy that changes silently invalidates everything decided under the old one. **The changed state never blocks reading and never asks for acceptance**, the document informs and the flow asks. **No legal copy is drafted anywhere** and the sweep fails any page that drafts one, because placeholder legal text reads as a document until somebody relies on it. **Six indexed URLs now answer the title-pattern question two ways**, `0.9` four times and `0.10` once following the placeholder brand and `6.1` refusing it. **Nodes `5.9` roll history and `5.10` profile written and drawn on 22 Aug 2026 in six pages, `D-78`, and neither node existed that morning.** **`D-36` added `5.9`, `5.10` and `5.11` on 20 August, said "stage 04 owes three more screens", and wrote none of them:** three registered files, three shell links, and nothing in `ia/docs/pages/`. **A row pointing at nothing at all is the dead item defect one level up**, and it survived because the registry entry looked like the work had been scheduled. **`5.11` stays unwritten** because its round 1 contents are `[?]` by that record's own words and asking is the instruction. **A row in `5.9` is a roll, not an item:** the baseline's four history tabs are inventory histories, which is an item grid with the roll removed, and until this node existed the only trace of a roll a person could reach was the item it produced. **No total of any kind**, and the cost is printed: a person cannot learn here whether they are up or down. **Both readings of `D-C` are drawn**, and the no reading gets its own page with the check control absent rather than present and broken. **`5.10` is an orphan and the page enforces that on itself**, six refusals from one rule, and it asks in its own open list whether it survives its orphan status at all. **And `0.13` owes three rows** that these nodes write out and do not edit in. **Node `3.4` was dissolved with the stock model and its page deleted**, and **node `3.1` the case catalogue drawn on 21 Aug 2026 in six pages**. **It jumped the queue because it existed as a link before it existed as a page:** the rail carries one destination, Cases, `D-40`, and it pointed at `catalogue.html` on all twenty two built screens while the file returned a 404. **And it could not be drawn as the node stood**, which is what `D-64` was for. Three things it refuses to invent and states on the surface: the category list, which nothing in this repository names and which rides on `D-D`; every risk band, because the thresholds are `[?]`; and the coin peg. **The daily free case is tile 1 and it is this node's open debt drawn**, and on the filtered state it leaves the grid, which is the price of `D-61` kept rather than worked around. **The live feed `0.8` renders in the shell on every page since `D-59`, and where it renders was decided by a measurement rather than by the decision:** put under the header it moved the case screen's act through the fold at 360x800, which node `3.3` forbids, so on mobile it sits after the content and on desktop the grid puts it back. **The case tile is artwork, name, risk and cost**, both markers gone with `D-60` and `D-61`. **Node 2.4 sign in was drawn on 21 August 2026 and its carrier changed twice before a pixel of it shipped.** `D-54` made it a dialog opened over the surface a person is already on, and `/signin` survived as the cold arrival. `D-55` gave it four providers against the node's own recorded argument, with the price printed beside the three that cannot receive a skin and three new states opened as debt on `5.3`, `5.1` and `4.1`. `D-56` split what the dialog carries from what the address carries, on the line of which half of the statement is finished. `D-57` took it from 877px tall to 659 with the picture moved beside the content and four sentences cut, none of which was doing work the node does not already do. **`D-58` took every disabled control off the surface** and left `D-26` enforced exactly as before: the press still signs nobody in, it just answers now, marking the declarations that are missing, naming which, and putting the keyboard on the first of them. It needs one clause of this file amended, and that clause is written out in the decision record and not yet applied. The card is built once in `_nav.js` because six copies of the one statement the node exists for is how five of them rot. **Node 2.4 sign in was drawn on 21 August 2026 and its carrier changed before a pixel of it existed:** `D-54` made it a dialog opened over the surface a person is already on, and `/signin` survived as the cold arrival that these five pages are. A dialog removes no step, and the node says so: the Steam round trip is unchanged and the gate still fires first. **What it removes is a loss**, the case and the count and the price going behind the person instead of behind the scrim, and the route defect in `flows.md` narrows from four screens to three rather than closing. The card is built once in `_nav.js` because six copies of the one statement the node exists for is how five of them rot. The grey contract, the shell and the global footer are drawn once and every screen inherits them. Home closed a two-instrument audit on 19 Aug 2026, nine findings confirmed and three withdrawn. **The case screen closed its two-instrument audit on 20 Aug 2026, twelve findings raised, ten confirmed and two withdrawn with reasons kept.** It is phase 1 plus eleven states, and its expected value, tested RTP and risk figure are all recomputable from the drop table printed under them. Eleven founder decisions rewrote it across two sessions, `D-30` to `D-40`: the price sits beside the act, the outcome is the item, the count switch has the reveal and outcome states it never had, the reveal stands vertical from two rolls in one row at every width, the sell control arrives with two of the node's own refusals reversed and their reasons kept, and My items left the rail for the account menu. **The audit's largest finding was in the scaffolding, not the screens:** the prototype panel took 220px of the left edge from 900px, so the product was being measured in a 445px column and every page including Home scrolled sideways from 900 to 1100. Fixed, and no page scrolls sideways at any of nine widths now. **The collapsed rail is drawn too**, a state `0.1` had specified since it was written with no page rendering it: the toggle is a menu icon rather than the chevron the wireframe had invented against Material's own quote, the rail goes 220 to 64 with tooltips on hover and on focus, and it no longer renders a second dead toggle below 900 where the header owns that control **Node `0.3` system pages drawn on 22 Aug 2026 in eight pages, `D-79`, and the registry held five states while the node had been asking for seven.** **The empty search result** has had its own row in that node's states table since it was written and was the only state of the node nobody could look at, and **the 500 with no carriers** had sat in its open list for four days with the owner column reading "stage 04 draws both versions": **a conditional block drawn once is drawn in whichever condition the person drawing it assumed.** **The response is drawn on every page and without it this node cannot be wireframed at all:** a soft 404 is HTTP 200 with a not-found body and Key-Drop's inversion is HTTP 404 with the home page in it, **both are the status and the body disagreeing**, and both are invisible in a drawing of the body alone. **The 503 made the node argue with itself:** the status-code table says `Retry-After` Yes for 503 and the 503 section says the unplanned window invents no number, and the RFC's own **MAY** is what resolves it rather than a preference of ours. **`D-59` had reached three files and left five, and they did not agree with each other either:** the component's own node carried the placement from before `D-31`, its page carried the one after it, and **nineteen wireframes carried a byte-identical comment saying the ticker was not on a page that was rendering it.** `0.8` now carries the register of every surface that refuses the feed and the parent of each, which it has owed since `D-77`: **fourteen pages refuse it and eighty carry it.** **And a decision record was printed on every page of the product:** the footer's last line read "published wherever money is spent, `D-28`" on all ninety four, and the `D-66` sweep that removed 31 citations from 17 files a day earlier read `main`, which the footer is not in. **So the one citation on every single page was the one the instrument could not see.** **Node `0.4` cookie consent drawn on 22 Aug 2026 in eight pages, `D-80`, and cluster 0 is complete: the only unbuilt page left in the whole product is `settings.html`.** **The one block this node said it could not place had been placed four days earlier and nobody told the node:** its section 8 read "`0.2` does not have one", the step 8 audit had already added **Cookie settings** to the footer's Company column quoting Article 7(3) from this node, and the finding was applied and never came back. **And the control did nothing:** a button with no handler on all ninety four pages, because `0.4` was unbuilt, **so the fix for Article 7(3) was itself the defect `D-58` forbids**, invisible to both nodes because one owns the carrier and the other owns what the carrier carries. **Article 7(3) is a rule about a count, so the count is measured:** giving is one press at every width, withdrawing is one at 1200 and **two at 360**, because below 900 that footer column is an accordion collapsed by default, and the difference is printed and sent back to `0.2`. **Symmetry is drawn as a construction rather than a resemblance:** one grid of equal columns, the same class on both, neither primary, on one line at every width, and the sweep measures all four at seven widths. **The manage page and the rejected page are the same screenshot and record different facts**, off because nothing was chosen against off because it was refused, and the record line is the only thing that separates them, which is what Article 7(1) is for. **Layer 2 held four of the node's ten blocks and had nowhere to be drawn.** **And the whole legal ground of this node has no parent in the three legal classes:** `CLAUDE.md` enumerates five compliance constraints and none of them is data protection, which is a candidate line at the stage close rather than a quiet widening of the third class. **Node `5.11` settings written and drawn on 22 Aug 2026 in four pages, `D-81`, and it closes the stage's page list.** **The input it was waiting for had been in the repository for four days.** `D-36` said its round 1 contents were `[?]` and that **the node owes that answer**; stage 04 read that as a question for the founder and put it to them twice, while `baseline-account.md` section 7 had carried all twenty rows of this screen since the founder's capture of 18 August. **What was missing was the derivation, not the input**, and a gate that cannot tell an absent source from an unfinished reading blocks work that was ready. **Nineteen of the twenty rows do not survive and almost none for a reason about settings:** eleven configure `LATER` modes or channels with no backlog row, five already live elsewhere on our map, three are refused and one is `[?]`. **The one that survives is why the node exists:** withdrawal to Steam is round 1, it works by sending a trade offer, a trade offer needs a trade URL, **and no node on the map held that field.** **Two rows are refused on a principle rather than on scope:** a switch that removes the confirmation before a value leaves the account removes a risk moment, and the answer to daily friction is a faster confirmation, not an absent one. **The anonymity control is stated and not drawn**, because it rewrites what `0.8` and `7.1` render and `7.1` has carried that question open since 21 Aug. **And the baseline changed under us between two dated captures**, Facebook to Discord, with neither record corrected because both are right on their own date. **Then six days of founder screen review, `D-82` to `D-87`, and the first of them found the instrument gap:** `_wf.css` had not parsed. Lines 460 to 486 were a superseded ticker draft that had lost all five of its selectors, CSS error recovery swallowed `.wf-main { padding-block }`, and **`padding-top` computed to `0px` on all 106 pages at three widths** while eight audit instruments read the file as text and saw nothing wrong. **A file that parses to fewer rules than it contains passes every class in the taxonomy**, so brace depth and orphan declarations are a contract-checklist line now with their own instrument. **`D-83` made vertical rhythm two rules rather than twenty missing ones**, after a second new instrument found twenty adjacent block pairs at 1440 with under 12px between them. **`D-84` gave cluster 5 the baseline's account band**, rendered once in `_nav.js` on all thirteen pages, **and printed two costs**: money renders twice on those pages, and the visible breadcrumb contradicts `0.13`'s "breadcrumb: none", so it ships with no schema and `0.13` owes a corrected row. **`D-85` rebuilt the inventory twice**, the first attempt having kept our own anatomy: two timestamped figures on every card, **which in a grid is a table with pictures.** The receipt moved into the hover panel where `B7-1` is actually read, the selection bar stopped hiding because four exits invisible until you guess that items are selectable is worse than an unavailable control that says why, and **the browser caught three defects reading would not have**, the sharpest being that `aspect-ratio` with `min-height` resolves by growing the width. **`D-86` reversed one of `D-81`'s refusals with its reason corrected:** the country control comes back as a statement the person makes, not as the geo control, because `D-23` decides access and nothing else does, **and a declaration with a person's name on it turns a false one into a ground for closing the account.** Three conditions carry it, and one is a walked contradiction: `baseline-account.md` 5b.6 found **two self-declared countries in one live account that disagree**, which with a ban on the other end bans people over the product's own contradiction. **`D-87` rebuilt `5.10` on the baseline's shape and it cost the node its own rule.** The profile said "nothing on this page may be the first home of a new capability" and the messages panel is one: no notifications row in `cjm-to-be.md`, no barrier, no job. **Amended rather than stepped over, on the same ground `D-36` put the page on, with `D-38`'s treatment: empty parent printed, no backlog row retro-fitted.** Half of it does have a parent, `B8-2`, six people in the ledger waiting 30 hours to almost three weeks, **and what every one of them describes is silence rather than slowness** - which is why `Delete all` exists on promo and not on system, since a product that lets you erase its own notice can afterwards say it told you. **And the five tiers do not travel:** `D-25` rule 3 says a tier that becomes an identity has stopped describing an entitlement, and this is the page whose whole subject is identity. **Then `D-88`, and it found that the node it amended had been measured against the wrong screen.** The founder chose all four of the baseline's history tabs after the argument for three was put and lost, and asking the question surfaced this: **the baseline has two history surfaces.** The public profile has four tabs and all four are mode histories, `baseline.md` 9.9; the account has five sub-tabs and they are transaction histories, `baseline-account.md` section 4, captured three days earlier. **`5.9` is a private account page and its baseline row cites the public one**, while the right source had sat in the repository for five days. **Nothing built on the wrong row was wrong, which is exactly why it survived:** both surfaces begin with an inventory history, so the node's own argument holds against either, and what the wrong row could not show is the three ledgers only the account surface has. **And the rule that let it happen is that `CLAUDE.md` requires a baseline row and does not say which surface it is about.** **Three of the four tabs then found a parent the node had not gone looking for:** rolls on `F3`, **deposits on `B4-3`, money leaves and does not arrive**, and **withdrawals on `B8-2`, waiting with numbers** - so the three-tab proposal had been refusing two capabilities with real barriers behind them, neither of them new. **The fourth ships as a rendered absence:** turning coins back into money is not a capability anywhere in `cjm-to-be.md` and the only capture of that tab is empty, so the panel prints both readings and names the one that already exists, rather than the baseline's `History is empty...` which is the pattern `0.5` and `3.2` both refuse. **The difference from a dead item is that a dead item promises something.** `/roll-history` becomes `/history`, and the browser caught two more defects reading would not have: a skin name in a code face, **`D-62` for the sixth time and the first visible in a render rather than in a measurement**, and a timestamp shredded into `14:4` and `0:07` at 360 by the wrap rule a reference needs and a figure must never have. **Then `D-89`, six founder rejections in one sitting, and five of them are one mistake wearing five faces: nothing was missing on any of those screens and everything was in the wrong layer.** The item card put a panel over the thing it acts on; the sort put four orders behind a press; history put four tabs inside one page, which is three states nobody could link to; the profile record repeated the identity the band above already carries; settings collected nineteen absences in a block at the end. **Each was defensible in prose and wrong on the screen, and the acceptance ritual passed all of them**, because its assertions ask about sideways scroll, one H1, a 44 pixel target and a wrapped label, **and none of them asks whether the thing looks like something a person would use.** **Two of the six were not taste, they were live defects, and the same assertions passed those too.** **`[hidden]` does nothing to an element with a display rule**, so the panel marked hidden went on rendering and the founder's screenshot shows Deposits selected with the rolls list underneath: **the sweep checked that the selected panel appeared and never that the other one had gone.** **And one account carried two names and two ids on one page**, the band reading Spectacle and ID 953709 while the record card ten pixels below read nightjar_cs and acc-7f3a91c4, **which is the third instance of one class** after the header reading 18.60 while the page read 130.60: **an assertion that reads one element cannot catch a contradiction between two.** **The fixes:** the item card takes the live product's composition with the artwork behind everything and the acts reserving their space by `visibility` so a card never grows on hover, measured identical at 458.28px in both states; **both sort keys sit on the screen with their direction and actually reorder the grid**; **`5.9` becomes four peer pages** with six new ones including **a withdrawal past our own published ceiling**, on the argument that earned `0.10` its place, that a published deadline with no state for its own failure is a number nobody has to meet; **`5.10` reads like a record and gains its main action, see yourself as a stranger does**, which opens one of the account's own results as a signed-out reader sees it and deliberately does not settle whether a public profile exists; and **`5.11` becomes a block per section with an icon slot, with the nineteen refusals moved out of the footnote into the section they belong to**, the rule unchanged that a row with no subject carries no control at all, **so you meet the absence where you went looking for the control.** **Then `D-90`, five founder rejections in one sitting, and the largest of them builds a page this project spent a node arguing against.** **`D-69` had left two carriers holding opposite verdicts on one object for two days:** `0.8` gave the feed's avatar a public profile as its destination and printed `public profile: no node yet` on every tile of every page, while `7.1` block 6 refused one outright because adding it would rebuild the trophy shelf that node was created to replace. **The founder chose the first, and the refusal is not deleted anywhere:** it stays in the page's own comment, in the node and in the record, with its reason attached, because it lost and it did not turn out to be wrong. **What answers it is on every card:** node `7.3` carries a route to its own round proof on every trophy, and there is no total, no rank and no tier anywhere on it, since a shelf with a total at the top is a leaderboard with one row. It refuses four things the baseline's own profile does: one counter rather than three with two of them reading `N/A`, the name printed once rather than twice, **a public id that is not the account id and whose format is `[?]` with opaque recommended**, and `noindex` because the page is about a person and letting a crawler hold a list of what somebody owns is a publication decision nobody made. **The open half of `D-69` moved and got sharper:** whether an account can turn this page off is decided nowhere, `D-58` forbids a toggle that does nothing, so `7.3` and `5.10` both carry the absence as a sentence and the owner is the founder. **On `5.10` three buttons went**, Settings, My items and History duplicating the account band's own tab strip ten pixels of scrolling above them, **the main action was repointed from a result to `player-owner.html`**, and **the messages list is bounded at 1000 and above only**, because an account with twenty messages made a profile page twenty messages long and a nested scroll region on a touch screen would trade a real defect for a tidy one. **`5.9` gained three states so every tab has its own set**, none of them a new subject: an open that did not finish, which `5.10` already sends a message about and which had nowhere to land; deposits with a boundary in force, `6.3` read from the ledger's side; and withdrawals on a restricted account, `G4` read from the same side. **And `5.1` is on its third build:** the skin gets its picture in the flow, field one of `0.6`, which the grid had put on a background layer where grey cannot be told apart from having no slot; the actions become an absolute overlay with a dim so rest and hover match by construction rather than by measurement; and the grid goes to three columns from 1000 and four from 1400 on a measured floor, `Send to Steam` at **100px for the label and 118px for the button against a 199px inner width**, measured in a browser at a 1000px viewport. **The 153px against 206px this line carried first was an estimate written in the voice of a measurement**, and it reached five files before anybody opened a browser on it; the conclusion did not move and the sentence claiming it was measured was false, which is the worse of the two. **The receipt line is deleted by founder decision and that is not a layout change: it is MVP row `F2` of `cjm-to-be.md`**, and the row is carried open rather than quietly edited, since if the refusal is only to the per-item copy then `F2` lives on `5.9`'s roll row, and if the value at the moment of the win is not retained at all then `5.9`'s returned column dies with it and `F2` has no home anywhere. **Nothing was retro-fitted:** the backlog still reads 40 MVP rows over 39 capabilities, `D-38`'s treatment. **And the fourth instance of one defect class:** the result pages called the account `Nightjar` while `WF_WHO` and every signed-in band call it `nightjar_cs`, **two renderings of one fact with no shared source, and the second one drifts because nothing makes it move.** **Every one of the seven findings came from the founder looking at a screen and none of them from an assertion**, one working day after the critique log recorded that the acceptance ritual asks about sideways scroll, one H1, a 44 pixel target and a wrapped label and asks nothing about whether the thing looks like something a person would use. **Five of the seven are mechanically checkable and the assertions are now written down; two are not**, and section 9 of `wireframes/docs/critique.md` says which and why: the receipt line was conformant to a backlog row that only the founder could know was unbuildable, and the repointed main action was correct when it shipped and became wrong when a decision landed. **One of the five assertions was then written out and run, and it returned a fifth instance of the class two decisions in a row call closed:** the account menu prints `Spectacle` on all 72 signed-in pages while `WF_WHO` and the account band on the same page read `nightjar_cs`, **so the `D-89` fix reached the band and never reached the menu that hangs off it.** **Fixed the same day, and the mechanism is the part worth keeping: `WF_WHO` had been declared halfway down `_nav.js`, below the function that renders the menu**, so the menu could not have used the fix even if `D-89` had looked at it, and a single source declared under one of its own readers is not a single source. It is declared above its first reader now, verified over all 120 pages at 72 menus and 0 disagreements with `profile-steam-down.html`'s override still resolving. **The cost stays printed rather than deleted by the fix:** two decisions in a row state this class is shut and it was live on 72 pages while both were being written, **and a fix that goes where the defect was seen rather than to every reader of the fact is how a class survives being closed twice.** **Then `D-91` on 23 August, and it is the largest thing this stage has been told:** **withdrawal on this product is a purchase and not a delivery.** What a person wins is credited at the site's own price, and taking the real skin out means the platform buys a real copy on the market, **so the difference settles against the balance in either direction.** The founder's capture of 18 August prints the whole mechanic in four columns and **the arithmetic closes exactly, 27.52 minus 4.48 is 23.04**; the 23 August capture is the same object at 51.33 minus 11.86, **and the number that settles is the price of a copy that can actually be bought, never the Steam listing, which stood 27 percent higher on that item.** **The gap is structural rather than a mistake:** a price inside a case is frozen when the case is priced, because re-pricing one item re-prices the case and moves the value of every unwithdrawn copy in every inventory on the platform, while the market re-prices itself daily, **and a thing that will not go away cannot be handled by hiding it.** **Node `5.3` put option A against option B on 18 August, marked it the largest open item on the node with the founder's name on it and said it had to be answered before stage 04 drew the node; stage 04 drew it five days ago and specified every figure against B, saying so on the page.** **The founder chose A: disclose it before the spend rather than at the till**, because B rewrites the treasury model and no row here can take that, **and A is what makes the trust positioning actually differentiated.** **The sentence the decision exists for:** inside the product the coin is self-consistent, win at 11.86 and sell back at 11.86, **and at the exit the same coin buys about four times less**, which is a second statement the published peg cannot express. **It lands on 23 pages across four surfaces and no page was created or deleted:** the item card gets the settlement line in the slot `D-90`'s deleted receipt vacated, with the rule said once above the grid rather than six times inside it; the case page gets a fifth figure for the per-case gap, **rendered as `[?]` because we do not hold that number and its owner is `0.11`**, plus the denomination of the expected value stated in words with no rename and no second percentage; the outcome carries one line under the receipt on all three roll states, **and `3.7` inherits it because the argument for `3.6` is about the item and not about the animation, and a page that carries a cost on one route and not the other teaches that the second route is cheaper**; and the withdrawal gets the baseline's own four columns plus the two things it lacks, **an as-of on the market price and the float on each copy, which is the first job field 15 of `0.6` has ever had.** **Three things it refuses to smooth:** the settlement goes both ways, since four of the nine walked rows sat above the market, **so a card that could only print a surcharge would be wrong on nearly half its rows**; no market is its own state and never a zero, since **`0.00` there would read as free**; and all seven withdrawal states carry a decision about the table rather than inheriting one, **three of them carrying no table at all with the reason printed, because a figure about a purchase that will not happen is the same defect as a zero.** **And this round's eight findings did not come from the founder or from a sweep: they came from agents building something else and reporting what they saw beside it.** The two prices with no sentence between them; **an empty state advertising the receipt deleted the day before, read by the one person with nothing to check it against**; a hard four column grid, **because a fixed column count is a promise about how many figures a block will ever hold**; **the flagship pre-spend statement shipped at the weight of a caveat, which is the defect the decision exists to close one level down**; **every `select` in the product with its caret wiped by two `background` shorthands, one of them carrying a comment that asserted the opposite**, which is how it survived a reading and why fixing one rule changed nothing; `48:00` printed twice beside a line saying the period is not published, **where both were true and a record of one offer had been written as a rule about all offers**; the missing line on `3.7`, **flagged by the agent that found it rather than filled, because the decision named `3.6`**; and an eighth found by counting, **block 7 rendered five figures on ten pages and four on three with no page saying why, left open rather than patched.** **Three of the eight an instrument could have caught and four are not checks at all**, and section 10 of `wireframes/docs/critique.md` says which and why. **The lesson is a second one rather than a repeat:** `D-90` said the ritual asks nothing about whether a screen looks usable, **and this round adds that a comment is a claim about behaviour and never evidence of it**, because a repository this heavy in comments makes reading a file feel like verification exactly where it is the opposite. **Counts after `D-91`: 120 registry entries against 121 html files, the difference being `overview.html`, both unchanged from `D-90`.** **`D-92`, the same day: the founder named the market every settlement is struck against and the answer is us.** The prices are ours, taken from Steam, and the market for these items is this product, **so both operands of every settlement are ours.** **Sixteen pages touched, none created, and no figure on any screen changed:** the line above the inventory grid on `5.1` and the market note on `5.3` now name us as the seller and Steam as the reference, the `[?]` on both moves from which market to **how far under Steam we set a copy**, the denomination clause on thirteen case screens says the copy price is also ours, **and one settlement clause moved into the passive on purpose** because the founder said "in principle" and where the copy comes from is still a question of fact. **The finding of the round is not a defect in a screen: closing an unknown can open a worse one.** Naming the venue made four `[?]` marks disappear and **invalidated the mechanism row `A1` used to make its number trustworthy**, since a link to our own listing proves nothing. **`0.11` section 2c restates `A1` as six requirements for this product and returns partly satisfied**, one met, one met in specification, one met on three surfaces and broken on two, one met as a route and wrong in its label, one partly, one not met, **which means `A1` is less satisfied on 23 August than it was on 22 August on the day four `[?]` marks closed.** **Two more came out of reading the drawn screens rather than the backlog:** the outbound link is drawn and points at Steam, so only its label is wrong, **and `A1`'s live market price is Steam on `1.0`, `3.3` and `3.6` and ours on `5.1` and `5.3`, one backlog row denoting two numbers with one word.** **Half of the risk band hole is answered too:** the three bands are an input when a case is built rather than a label derived from its drop table, and **the thresholds are still `[?]`.** Section 11 of `wireframes/docs/critique.md` and section 73 of `wireframes/docs/screens.md`. **Then `D-93` on 24 August: the founder confirmed the venue and closed seven open points in one pass.** The hedge in `D-92` is spent, and the answers are: **publish the per-case gap figure** with a decided method and cadence and the value moving from `0.11` to the founder; **do not rename tested return**, because a second figure beside it does the work a new name only promises; **an expired trade offer re-strikes at the price of the day**, 4.90 coins more on the drawn record, printed above the button rather than in the receipt; **a person can hide their public profile**, default visible, with the two things it does not take back printed on the row; **Cash out becomes the sell-back ledger**, which was the only movement of value on the product with no record of its own; **`cjm-to-be.md` gains nothing**, because retro-fitting is what `D-38` forbids by name; and **`R7` stays a finding**, because a price nobody can pay is the dead item defect written as a number. **Two pages created, twenty-nine touched, and the round's own finding is a measurement rather than a screen:** `D-92` claimed every venue absence had been replaced and **eleven pages still carried one three days later**, because the count was taken from what was edited rather than from what carried the string. **Nine of those eleven were a different venue question and the first fix on them was wrong**, caught before commit and reverted with the reason kept. **Then `D-94` on 25 August: the deposit bonus returns to round 1 and the badge goes on the header control**, the founder overruling `navigation.md` rule 4 after it was put to them. **The refusal is kept whole rather than deleted**, because it lost rather than turned out to be wrong: `cjm-to-be.md` cuts any bonus until case mathematics are modelled and **that model still does not exist**. What the founder put against it is in our own research, `aarrr.md` recording a match or first-credit offer on every competitor in the bank, **and the finding is the asymmetry: the refusal priced the risk of having a bonus and priced nothing for not having one.** Three things bind the badge, **the cap travels with the percentage in the control's accessible name**, the whole offer is restated on `4.1` from one declaration read by both surfaces, and the bonus carries **zero wagering** so `C4` survives unchanged. **Thirteen pages declare `bonus: false`**, eleven of which render a deposit control at all: `4.2`, whose own forbidden list reads "no offer of any kind", the five `6.1` surfaces, and the seven error pages. **And the deposit screen now carries two absences side by side**, what the money buys and what the bonus adds, both blocked on the same unpublished peg, which is the sharpest argument yet for closing it. |
| 05 | Voice | voice | Not started |
| 06 | Concept | directions, concept | Not started |
| 07 | UI + Visual | overview, index, sample screens | Not started |
| 08 | Tokens + Components | kit overview, foundations, component pages | Not started |
| 09 | Design System | why, patterns, states | Not started |
| 10 | Responsive | defined by the stage | Not started |
| 11 | Animation | defined by the stage | Not started |
| 12 | Handoff | defined by the stage | Not started |

**Stage 01 critique: done and closed.** It ran on two instruments in August 2026 and produced 22 confirmed findings, 2 withdrawn on verification. All 22 are now fixed, and the summary is visible on the research page under "Before and after" (D-10).

**The last one, closed by re-running step 4.** The benchmark had scored five same-category competitors, while the stage defines it as one dimension measured against four to five best-in-class products from **other** categories. It could not be edited into shape, so it was re-run on 5 August 2026. Dimension chosen by the founder: **the reveal moment**. Products: Pokémon TCG Pocket, MONOPOLY GO!, EA SPORTS FC 26 Ultimate Team, Genshin Impact, Nike SNKRS. Six criteria, all verifiable from pages opened in a browser that day, with screenshots in `research/screens/benchmark/`. The in-category trust scoring is retained as Part 2 of the same file, renamed for what it is, with its relocation into `competitors.md` owned by CJM step 4 (D-11).

One thing the re-run could not deliver, named rather than hidden: no product in the set documents the craft of its reveal publicly, because probability disclosure is legally forced in several jurisdictions and craft is not. Timing, cut, sound and hold get designed at stage 06, not matched.

**CJM stage: done, August 2026.** `cjm-as-is.md` is the research half: the input inventory, the eight phases of the real path, and on each phase an emotion with a counted source or a `[?]`, plus 8 barrier classes, an emotional curve that breaks rather than interpolates where no signal exists, and 5 growth zones. `cjm-evidence.md` holds the quotes each pattern is counted from. `cjm-to-be.md` is the design half: the same eight phases rebuilt, six divergences run diverge-then-converge, every capability traced to a barrier, a job or a named constraint, and one backlog of 38 MVP rows over 37 distinct capabilities that refines the MVP core instead of forking it. Both halves went through a two instrument critique, 40 rows on As-Is and 37 on To-Be. The stage closed with two founder decisions on 11 August 2026: the responsible play page returns to round 1, and the daily free case enters the MVP as row I2 with its missing parent stated rather than dressed up.

**The one line conclusion.** The deepest pit is not the open, it is everything after it: P7 the outcome and P8 payoff or exit both sit at -5, on patterns of 7, 11 and 12 people, while the open itself has no counted signal at all. To-Be closes that end of the path with the instance value receipt that follows the item into inventory and onto the withdrawal record, a public withdrawal clock with per-state timers and our own published p90, and the rule that the sum required to withdraw is stated before the deposit and can never rise.

**IA base layer: done, August 2026.** Sixteen entities before any screen, twelve MVP screens in seven intent clusters against thirteen LATER, six flows in Mermaid with states and dead ends rather than happy paths, and the main job at three taps. The tracing matrix carries a scope row and a budget line, and both critiques ran on two instruments: 31 findings, Codex 13, Claude 13, both 5, with 25 fixed and the withdrawn ones visible with their reasons. **One thing it decided did not survive the detail layer.** The base layer flattened the baseline's nine item rail into a header of four items; on 11 August 2026 the founder reversed that at node 0.1. The model is the baseline's own two carriers, a rail that owns destinations and a header that owns money in two figures, with the rail filled by round 1's live items only. Reasoning: `docs/decisions.md` D-19 and D-21.

**Stage 03 input gate: passed on 11 August 2026, and it found more than it expected.** All seven declared inputs exist. The four decisions the CJM map could not close were answered (D-17): the age gate becomes two layers, a declaration at registration plus identity verification before funding, **and `D-26` later parked the second layer out of round 1 and moved it to before withdrawal**; the roll history migration goes to the platform as a question of fact; the internal weights check is scheduled before stage 04; inventory capital is deferred to 03b. The founder also decided the colour register is taken from the baseline rather than derived near it (D-18), which reverses part of D-03. Then the gate found that "the structure is inherited" had no source document: seven things were listed as inherited and none of them was written down. That produced `baseline.md`, a live walk of daddyskins.com on public pre-login pages, whose sharpest finding is that **only one of the nine left rail destinations is in round 1**.

## Scope

**Round 1:** home, case opening, registration and account, deposit, skin withdrawal to Steam, provably fair page with verification tool, geo block layer, responsible play page, public result page, roll history, profile, settings. Twelve surfaces since `D-36` of 20 August 2026, and before that the list moved twice on 11 August 2026, both times by founder decision: responsible play was restored after the CJM stage found it named in the Lean UX canvas and dropped from both scope lists with no verdict, and the public result page was added at the 03b gate because row `F4` puts a shared object into the world and nothing said what that object was.

**Later:** case battles, gunfights, upgrades, in-platform exchange, giveaways, leaderboards, referral programme, rakeback.

## Locked decisions

| Decision | Value |
|---|---|
| Language | One, English |
| Existing brand | Own logo. **Colour register inherited from the baseline** by founder decision, 11 Aug 2026, D-18 |
| Existing design system | None |
| Baseline inheritance | Structure, IA and colour. Typography, shape, motion and copy are ours |

Full reasoning and consequences: `CLAUDE.md`. Decision records: `docs/decisions.md`.

## Sources

```
research/docs/
├── competitors.md      competitor groups (hard, soft, aspirational) and matrix
├── benchmark.md        Part 1 reveal-moment benchmark (out of category), Part 2 retained trust matrix
├── aarrr.md            AARRR funnel, one metric and one decision per stage
├── ux-patterns.md      behavioural patterns and the chosen pattern
├── lean-ux-canvas.md   Lean UX Canvas v2 (Gothelf), glance view of the foundation
├── research.md         synthesis, single source of truth
├── personas.md         four behavioural personas, one primary
├── jtbd.md             JTBD hierarchy, matrix, MVP core refined at the CJM stage
├── cjm-as-is.md        As-Is journey: 8 phases, emotions with sources, barriers, growth zones
├── cjm-evidence.md     the counted quotes behind every As-Is pattern
├── cjm-to-be.md        To-Be journey, divergences, backlog and MVP core
├── baseline.md         daddyskins.com walked live: inventory, navigation, composition, colour
├── strategy.md         objectives, segments, business model, four locked founder decisions
├── live-research.md    post-persona verification: 5 confirmed, 1 killed, 2 unresolved
├── quality-check.md    quality gate, claim verification
└── product-model.md    superseded by strategy.md, kept for history
ia/docs/
├── sitemap.md          entities, detailed node map (clusters 0..8), navigation, tracing matrix
├── flows.md            six user flows in Mermaid: decisions, states, both ends
├── critique.md         both critique passes, two instruments, with a found-by column
└── pages/              one md per file-level node, 26 planned
    ├── navigation.md   node 0.1, the two carriers: rail and header, state matrix, anatomy, SEO and a11y
    └── footer.md       node 0.2, the second interlinking plane, the trust strip and the legal ground
research/screens/            competitor screenshots
research/screens/baseline/   daddyskins.com baseline screenshots
research/screens/benchmark/  out-of-category reveal-moment sources
```

The last four files are migration residue: they carry real research but do not map onto a course artefact yet. Their fate is recorded in `docs/decisions.md` and settled in the next round.

## Repo layout

```
index.html          project roadmap, entry point
_nav.js             the one navigation registry: stages, pages, done flags
_nav.css            the one sidebar theme
CLAUDE.md           rules that must hold next session, no status
AGENTS.md           entry point for the Codex critic
docs/decisions.md   decision records, never auto loaded
research/           stages 01, 02 and 02+
```

The stage packs live outside this repo, in `AI Design Workflow/`. Nothing from there is committed here.
